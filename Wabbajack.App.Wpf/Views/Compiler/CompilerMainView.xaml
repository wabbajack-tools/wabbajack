<rxui:ReactiveUserControl
    x:Class="Wabbajack.CompilerMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lib="clr-namespace:Wabbajack;assembly=Wabbajack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Wabbajack"
    xmlns:mahapps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rxui="http://reactiveui.net"
    xmlns:wabbacommon="clr-namespace:Wabbajack.Common;assembly=Wabbajack.Common"
    xmlns:controls1="clr-namespace:Wabbajack.ViewModels.Controls"
    xmlns:ic="clr-namespace:FluentIcons.WPF;assembly=FluentIcons.WPF"
    d:DataContext="{d:DesignInstance local:CompilerMainVM}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="local:CompilerMainVM"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:LogView x:Name="LogView" Grid.RowSpan="2" Margin="0, 0, 16, 0" />
        <local:DetailImageView x:Name="DetailImage" Grid.Column="1" BorderThickness="0" TitleFontSize="28" AuthorFontSize="13" />
        <local:CpuView x:Name="CpuView" Grid.Column="1" Grid.Row="1" ViewModel="{Binding}" />
        <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0, 16, 0, 0">
            <local:WizardButton x:Name="PrevButton" Text="Back to File Manager" Icon="ChevronLeft" Command="{Binding PrevCommand}" Direction="RightToLeft" HorizontalAlignment="Left" />
            <local:WizardButton x:Name="CancelButton" Text="Cancel" Icon="Dismiss" Command="{Binding CancelCommand}" Direction="RightToLeft" HorizontalAlignment="Left" />
            <local:WizardButton x:Name="OpenFolderButton" Text="Open folder" Icon="Folder" Command="{Binding OpenFolderCommand}" HorizontalAlignment="Right" UseAltStyle="True" />
            <local:WizardButton x:Name="PublishButton" Text="Publish" Icon="CloudArrowUp" Command="{Binding PublishCommand}" HorizontalAlignment="Right" UseAltStyle="True" />
            <local:WizardButton x:Name="StartButton" Text="Start" Icon="PlayCircle" Command="{Binding StartCommand}" HorizontalAlignment="Right" UseAltStyle="True" />
        </Grid>
    </Grid>
</rxui:ReactiveUserControl>

<rxui:ReactiveUserControl
    x:Class="Wabbajack.ManualDownloadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wabbajack"
    xmlns:mahapps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rxui="http://reactiveui.net"
    xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
    d:DesignHeight="400"
    d:DesignWidth="500"
    x:TypeArguments="local:ManualDownloadsVM"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ArchiveRowResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="StepText" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="Margin" Value="0, 0, 0, 3" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition x:Name="ListRow" Height="0" />
        </Grid.RowDefinitions>

        <!-- The card scrolls when the window is short; the list below keeps its own virtualized scrolling. -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Top">
        <local:AttentionBorder x:Name="Card" ClipToBounds="True">
            <Grid Margin="16, 10, 16, 10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="HeaderText" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="SizeText" Grid.Column="1" FontSize="12" Foreground="{StaticResource ComplementaryWhite25Brush}" VerticalAlignment="Center" Margin="12, 0, 0, 0" />
                </Grid>

                <Grid x:Name="CardBody" Grid.Row="1" Margin="0, 8, 0, 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="FileNameText" Grid.Row="0" FontSize="14" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0, 4, 0, 8">
                        <TextBlock Text="from" FontSize="12" Foreground="{StaticResource Transparent66ForegroundBrush}" VerticalAlignment="Center" />
                        <Border CornerRadius="4" Background="{StaticResource ComplementaryPrimary16Brush}" Padding="6, 2" Margin="8, 0, 0, 0" VerticalAlignment="Center">
                            <TextBlock x:Name="SiteNameText" FontSize="11" Foreground="{StaticResource ForegroundBrush}" />
                        </Border>
                        <TextBlock x:Name="InstructionsText" FontSize="12" Foreground="{StaticResource Transparent66ForegroundBrush}" VerticalAlignment="Center" Margin="10, 0, 0, 0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource StepText}" Text="1. Open the download page in your browser" />
                            <TextBlock Style="{StaticResource StepText}" Text="2. Start the download there" />
                            <TextBlock Style="{StaticResource StepText}" Text="3. Wait here, the file is picked up automatically" />
                        </StackPanel>
                        <local:WJButton x:Name="OpenPageButton" Grid.Column="1" ButtonStyle="Color" Text="Open page" Icon="Open" IconSize="16" Width="150" Height="32" VerticalAlignment="Top" Margin="16, 0, 0, 0" />
                    </Grid>

                    <Grid Grid.Row="3" Margin="0, 6, 0, 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid Width="18" Height="18" VerticalAlignment="Top" Margin="0, 1, 0, 0">
                            <mahapps:ProgressRing x:Name="WatchRing" Width="18" Height="18" MinWidth="18" MinHeight="18" Foreground="{StaticResource PrimaryBrush}" IsActive="True" />
                            <ic:SymbolIcon x:Name="WatchIcon" FontSize="18" IconVariant="Filled" Symbol="CheckmarkCircle" Foreground="{StaticResource SuccessBrush}" Visibility="Collapsed" />
                        </Grid>
                        <TextBlock x:Name="WatchStatusText" Grid.Column="1" FontSize="13" Margin="10, 0, 0, 0" VerticalAlignment="Center" TextWrapping="Wrap" />
                    </Grid>

                    <TextBlock x:Name="NoticeText" Grid.Row="4" FontSize="12" Foreground="{StaticResource Transparent66ForegroundBrush}" Margin="28, 4, 0, 0" TextWrapping="Wrap" Visibility="Collapsed" />

                    <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="28, 4, 0, 0">
                        <TextBlock x:Name="WatchFolderText" FontSize="12" Foreground="{StaticResource ComplementaryWhite25Brush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="360" />
                        <local:WJButton x:Name="ChangeFolderButton" ButtonStyle="Transparent" Text="Change folder" Icon="FolderOpen" IconSize="14" Width="140" Height="24" Margin="4, 0, 0, 0" />
                    </StackPanel>

                    <Grid Grid.Row="6" Margin="0, 10, 0, 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <local:WJButton x:Name="RetryButton" Grid.Column="1" ButtonStyle="Mono" Text="Try again" Icon="ArrowCounterclockwise" IconSize="16" Width="130" Height="32" Margin="0, 0, 8, 0" Visibility="Collapsed" />
                        <local:WJButton x:Name="SkipButton" Grid.Column="2" ButtonStyle="Mono" Text="Skip for now" Icon="ArrowForward" IconSize="16" Width="150" Height="32" Margin="0, 0, 8, 0" />
                        <local:WJButton x:Name="PickFileButton" Grid.Column="3" ButtonStyle="Mono" Text="I downloaded it elsewhere" Icon="DocumentSearch" IconSize="16" Width="230" Height="32" />
                    </Grid>
                </Grid>
            </Grid>
        </local:AttentionBorder>
        </ScrollViewer>

        <local:WJButton x:Name="ShowAllButton" Grid.Row="1" ButtonStyle="Transparent" Text="Show all" Icon="ChevronDown" IconSize="14" Direction="RightToLeft" Width="130" Height="28" HorizontalAlignment="Left" Margin="0, 4, 0, 0" />

        <Border x:Name="ListBorder" Grid.Row="2" CornerRadius="8" BorderThickness="0" Background="White" Margin="0, 8, 0, 0" Visibility="Collapsed">
            <Grid Margin="-1">
                <Border x:Name="ListMask" CornerRadius="8" Background="White" Margin="1" />
                <Grid>
                    <Grid.OpacityMask>
                        <VisualBrush Visual="{Binding ElementName=ListMask}"/>
                    </Grid.OpacityMask>
                    <ListBox
                        x:Name="RowsList"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        AlternationCount="2"
                        ItemTemplate="{StaticResource ArchiveRowTemplate}"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.CanContentScroll="True"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        VirtualizingPanel.ScrollUnit="Pixel">
                        <ListBox.Resources>
                            <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource ListBoxItemStyle}">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Style.Triggers>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                        <Setter Property="Background" Value="{StaticResource ComplementaryPrimary08Brush}"/>
                                    </Trigger>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="{StaticResource LogViewAlternateBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.Resources>
                    </ListBox>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</rxui:ReactiveUserControl>

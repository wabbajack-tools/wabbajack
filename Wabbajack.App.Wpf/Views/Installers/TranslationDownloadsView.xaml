<rxui:ReactiveUserControl
    x:Class="Wabbajack.TranslationDownloadsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wabbajack"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rxui="http://reactiveui.net"
    xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
    d:DesignHeight="600"
    d:DesignWidth="900"
    x:TypeArguments="local:TranslationDownloadsVM"
    mc:Ignorable="d">
    <Grid Margin="16" MaxWidth="1100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0, 0, 0, 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ic:SymbolIcon Symbol="Translate" FontSize="24" IconVariant="Filled" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0, 0, 12, 0" />
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Name="ExplanationText" FontSize="13" Foreground="{StaticResource Transparent66ForegroundBrush}" TextWrapping="Wrap" Margin="0, 2, 0, 0" />
            </StackPanel>
        </Grid>

        <local:ManualDownloadView x:Name="DownloadsView" Grid.Row="1" />

        <local:WJButton x:Name="ContinueButton" Grid.Row="2" Text="Continue" Icon="ArrowRight" ButtonStyle="Color" HorizontalAlignment="Right" Margin="0, 16, 0, 0" />
    </Grid>
</rxui:ReactiveUserControl>
